apiVersion: apps/v1
kind: Deployment
metadata:
  name: {APP_NAME}
spec:
  template:
    spec:
      containers:
      - name: {APP_NAME}
        resources:
          requests:
            memory: "256Mi"
            cpu: "500m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
