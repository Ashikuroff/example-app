apiVersion: apps/v1
kind: Deployment
metadata:
  name: {APP_NAME}
spec:
  template:
    spec:
      containers:
      - name: {APP_NAME}
        resources:
          requests:
            memory: "128Mi"
            cpu: "250m"
          limits:
            memory: "256Mi"
            cpu: "500m"
