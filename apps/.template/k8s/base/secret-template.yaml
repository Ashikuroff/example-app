# This is a TEMPLATE file for sealed-secrets
# DO NOT commit actual secrets to this file
# Instead, follow the ONBOARDING.md guide to generate sealed secrets using kubeseal
# Example:
# echo -n 'your-api-key' | kubectl create secret generic {APP_NAME}-secret --from-literal=api_key=- --dry-run=client -o yaml | kubeseal -f - -w secret.yaml
#
# Then uncomment and update the contents below after sealing in the correct cluster

apiVersion: v1
kind: Secret
metadata:
  name: {APP_NAME}-secret
  labels:
    app: {APP_NAME}
type: Opaque
stringData:
  api_key: "CHANGE_ME_USE_KUBESEAL"
  database_url: "CHANGE_ME_USE_KUBESEAL"
