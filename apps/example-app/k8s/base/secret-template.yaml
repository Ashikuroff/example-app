# This is a TEMPLATE file for sealed-secrets
# DO NOT commit actual secrets to this file
# Instead, follow the ONBOARDING.md guide to generate sealed secrets using kubeseal
# Example:
# echo -n 'your-api-key' | kubectl create secret generic example-app-secret --from-literal=api_key=- --dry-run=client -o yaml | kubeseal -f - -w sealed-example-app-secret.yaml
#
# Then rename to secret.yaml after sealing

apiVersion: v1
kind: Secret
metadata:
  name: example-app-secret
  labels:
    app: example-app
type: Opaque
stringData:
  api_key: "CHANGE_ME_USE_KUBESEAL"
  database_url: "CHANGE_ME_USE_KUBESEAL"
